export interface SymbolDetails {
  symbol: string;
  lastPrice: string;
  highPrice24h: string;
  lowPrice24h: string;
  volume24h: string;
  turnover24h: string;
  // Add other relevant properties based on the Bybit API response
}
